version: 2
updates:
  # Go modules (CLI + build tooling declared via `go tool`)
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    groups:
      go-dependencies:
        patterns:
          - "*"
    labels:
      - "ok-to-test"
      - "area/dependency"
